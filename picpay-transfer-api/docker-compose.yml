version: '3.8'

services:
  api:
    build: .
    container_name: picpay-transfer-api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
